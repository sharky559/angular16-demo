import { PayPlatformPipe } from './pay-platform.pipe';

describe('Pipe: PayPlatformIcone', () => {
  it('create an instance', () => {
    let pipe = new PayPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
