import { PayStatusTranslatePipe } from './pay-status-translate.pipe';

describe('Pipe: PayStatusTranslatee', () => {
  it('create an instance', () => {
    let pipe = new PayStatusTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
