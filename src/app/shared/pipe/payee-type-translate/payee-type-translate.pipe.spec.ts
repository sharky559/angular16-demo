import { PayeeTypeTranslatePipe } from './payee-type-translate.pipe';

describe('Pipe: PayeeTypeTranslatee', () => {
  it('create an instance', () => {
    let pipe = new PayeeTypeTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
